import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sunny.migrainetracker',
  appName: 'Migraine Tracker',
  webDir: 'dist'
};

export default config;
